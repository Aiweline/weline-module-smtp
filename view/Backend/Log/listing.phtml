<?php
/**
 * 框架 fetch() 期望模板位于 `view/Backend/...`。
 * 本模块实际模板放在 `view/templates/Backend/...`，这里转发兼容。
 *
 * 注意：框架会把模板编译到 `view/tpl/{lang}/...` 后再执行，
 * 不能使用 __DIR__ 相对定位；这里使用 BP 拼出绝对路径确保可 include。
 */
require BP . 'app/code/Weline/Smtp/view/templates/Backend/Log/listing.phtml';

